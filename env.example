# Environment Variables Configuration

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Replace with your actual MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/autoguru

# JWT Configuration
# IMPORTANT: Change this secret key in production to a strong, random string
JWT_SECRET=autoguru-secret-key-change-in-production
JWT_EXPIRES_IN=24h

# CORS Configuration (optional)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
